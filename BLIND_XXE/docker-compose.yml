services:
  blind-api:
    image: python:3.9-slim
    ports:
      - "5000:5000"
    working_dir: /app
    volumes:
      - .:/app
    command: sh -c "pip install flask lxml==4.6.2 && python app.py"